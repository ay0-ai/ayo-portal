# Deployment moved to ay0-ai/erp repo.
# This workflow is intentionally disabled.
name: CI/CD (deprecated)

on:
  workflow_dispatch: # manual-only, effectively disabled

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD has moved to the ay0-ai/erp repo."
